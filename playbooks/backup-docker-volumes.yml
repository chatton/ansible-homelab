---
- hosts: qnap
  become: true
  pre_tasks:
  - name: Include vault variables.
    include_vars: '../{{vault_file}}'
    tags: [always]
  roles:
  - role: docker_backup
    vars:
      container_backup: linkding
